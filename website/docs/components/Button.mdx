---
title: 按钮
description: 按钮是由用户使用鼠标、键盘、手指、语音指令或其他辅助技术激活的交互式组件。一旦被激活，它就会执行一个可编程的操作，例如提交一个表单或打开一个对话框。
---

## 用法
<Showcase component={'Button'} componentProps={componentProps} />

## 样式
```tsx live editable
<Stack className={'space-x-3'}>
    <Button>按钮</Button>
    <Button variant={'outline'}>按钮</Button>
    <Button variant={'light'}>按钮</Button>
    <Button variant={'subtle'}>按钮</Button>
    <Button variant={'link'}>按钮</Button>
</Stack>
```

## 颜色
```tsx live editable
<Stack className={'space-x-3'}>
    <Button>按钮</Button>
    <Button color={'green'}>按钮</Button>
    <Button color={'yellow'}>按钮</Button>
    <Button color={'red'}>按钮</Button>
    <Button color={'slate'}>按钮</Button>
</Stack>
```

## 状态
```tsx live editable
<Stack className={'space-x-3'}>
    <Button>按钮</Button>
    <Button as={'span'} disabled color={'red'}>按钮</Button>
    <Button color={'natural'}>按钮</Button>
    <Button color={'warning'}>按钮</Button>
    <Button color={'danger'}>按钮</Button>
</Stack>
```

## 属性
<PropsTable componentProps={componentProps} />
