---
title: 数字输入框
description: 仅允许输入数字格式的输入框。
---

## 用法

<Showcase componentName="NumberInput" />

## 值限制

```tsx preview editable
<div className={'flex flex-row items-center gap-3'}>
  <NumberInput max={10} />
  <NumberInput min={10} />
  <NumberInput min={10} max={20} />
</div>
```

## 格式化

```tsx preview editable
<div className={'flex flex-row items-center gap-3'}>
  <NumberInput
    parse={(value) => value.replace(/\$\s?|(,*)/g, '')}
    format={(value) => (!Number.isNaN(value) ? `$ ${value}`.replace(/\B(?=(\d{3})+(?!\d))/g, ',') : '$ ')}
  />
</div>
```

## 前缀

```tsx preview editable
<div className={'flex flex-row items-center gap-3'}>
  <NumberInput prefix={<Computer />} />
</div>
```

## 大小

```tsx preview editable
<div className={'flex flex-row items-center gap-3'}>
  <NumberInput size={'xs'} />
  <NumberInput size={'sm'} />
  <NumberInput size={'md'} />
  <NumberInput size={'lg'} />
  <NumberInput size={'xl'} />
</div>
```

## 滚轮改变

```tsx preview editable
<div className={'flex flex-row items-center gap-3'}>
  <NumberInput mouseWheel />
</div>
```

## 回调事件

```tsx preview editable
<div className={'flex flex-row items-center gap-3'}>
  <NumberInput mouseWheel onChange={(value) => console.log(value)} />
</div>
```

## 自定义控件

```tsx preview editable
function Demo() {
  const controls = React.useRef();

  return (
    <div className={'flex flex-row items-center gap-3'}>
      <Button
        onClick={() => controls.current.decrement()}
        color={'gray'}
        variant={'light'}
        className={'!px-0 aspect-square'}
      >
        <Minus />
      </Button>

      <NumberInput hideControls controlsRef={controls} onChange={(value) => console.log(value)} />

      <Button
        onClick={() => controls.current.increment()}
        color={'gray'}
        variant={'light'}
        className={'!px-0 aspect-square'}
      >
        <Plus />
      </Button>
    </div>
  );
}
```

## 属性

<PropsTable componentName="NumberInput" />
