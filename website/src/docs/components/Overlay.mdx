---
title: 覆盖层
description: 覆盖给定的元素
---

## 导入

```tsx
import { Overlay } from '@agile-ui/react';
```

## 用法

```tsx preview editable
function OverlayDemo() {
  const [visible, setVisible] = React.useState(false);

  return (
    <>
      <div className={'relative h-32 flex items-center justify-center'}>
        {visible && <Overlay />}
        <Button color={visible ? 'red' : 'green'}>
          {!visible ? '这是一个可以随意点击的按钮' : '覆盖层开启后就无法点击了'}
        </Button>
      </div>

      <div className={'flex mt-3 justify-center'}>
        <Button onClick={() => setVisible((v) => !v)}>切换覆盖层</Button>
      </div>
    </>
  );
}
```

## 圆角

```tsx preview editable
<div className={'relative h-32 flex items-center justify-center'}>
  <Overlay radius />
  <Button>圆角效果</Button>
</div>
```

## 颜色

```tsx preview editable
<>
  <div className={'relative h-32 flex items-center justify-center'}>
    <Overlay color={'blue'} />
    <Button color={'gray'}>蓝色覆盖</Button>
  </div>
  <div className={'relative h-32 flex items-center justify-center'}>
    <Overlay color={'white'} />
    <Button>白色覆盖</Button>
  </div>
</>
```

## 模糊

```tsx preview editable
<div className={'relative h-32 flex items-center justify-center'}>
  <Overlay blur={1} />
  <Button>模糊效果</Button>
</div>
```

## 属性

<PropsTable componentName="Overlay" />
