---
title: 按钮
description: 按钮组件用于触发动作或事件，例如提交表单、打开对话框、取消动作或执行删除操作。
---

## 用法

## 样式

```tsx preview editable
<div className={'flex flex-row gap-3'}>
  <Button>按钮</Button>
  <Button variant={'outline'}>按钮</Button>
  <Button variant={'light'}>按钮</Button>
  <Button variant={'subtle'}>按钮</Button>
  <Button variant={'link'}>按钮</Button>
</div>
```

## 颜色

```tsx preview editable
<div className={'flex flex-row gap-3'}>
  <Button>按钮</Button>
  <Button color={'green'}>按钮</Button>
  <Button color={'yellow'}>按钮</Button>
  <Button color={'red'}>按钮</Button>
  <Button color={'gray'}>按钮</Button>
</div>
```

## 大小

```tsx preview editable
<div className={'flex flex-row gap-3'}>
  <Button size={'xs'}>按钮</Button>
  <Button size={'sm'}>按钮</Button>
  <Button>按钮</Button>
  <Button size={'lg'}>按钮</Button>
  <Button size={'xl'}>按钮</Button>
</div>
```

## 图标按钮

```tsx preview editable
<div className={'flex flex-row gap-3'}>
  <Button>
    <Config className={'mr-1'} /> 设置
  </Button>
  <Button className={'!px-0 aspect-square'}>
    <Config />
  </Button>
  <Button className={'!px-0 aspect-square'} variant={'outline'} color={'gray'}>
    <Github />
  </Button>
  <Button className={'!px-0 aspect-square'} radius={'full'} color={'yellow'}>
    <Github />
  </Button>
  <Button className={'!px-0 aspect-square'} variant={'light'} color={'red'}>
    <Sun />
  </Button>
  <Button className={'!px-0 aspect-square'} variant={'subtle'} color={'lime'}>
    <Moon />
  </Button>
  <Button className={'!px-0 aspect-square'} variant={'subtle'} color={'lime'}>
    <span>
      <Moon />
    </span>
  </Button>
</div>
```

## 状态

```tsx preview editable
<div className={'flex flex-row gap-3'}>
  <Button disabled>按钮</Button>
  <Button loading>按钮</Button>
  <Button active>按钮</Button>
  <Button fullWidth>按钮</Button>
</div>
```

## 按钮组

```tsx preview editable
<div className={'flex flex-row gap-3 items-center'}>
  <ButtonGroup>
    <Button>保存</Button>
    <Button className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup disabled>
    <Button>保存</Button>
    <Button className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup>
    <Button>保存</Button>
    <Button variant="outline" className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup>
    <Button>保存</Button>
    <Button color={'gray'} className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup variant="outline">
    <Button>保存</Button>
    <Button className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup variant="light">
    <Button>保存</Button>
    <Button className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup vertical>
    <Button>保存</Button>
    <Button color={'green'} className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup vertical variant="outline">
    <Button>保存</Button>
    <Button className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
  <ButtonGroup vertical variant="light">
    <Button>保存</Button>
    <Button className={'!px-0 aspect-square'}>
      <Plus />
    </Button>
  </ButtonGroup>
</div>
```

## 属性

<PropsTable componentName="Button" />
